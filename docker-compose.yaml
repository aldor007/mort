version: '3'

services:
  mort:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - TAG=dev
        - COMMIT=master
        - DATE=now
    restart: always
    ports:
      - 8080:8080
